using ApproxiMATE.Helpers;
using ApproxiMATE.Services.Interfaces;
using ApproxiMATE.ViewModels.Base;
using System;
using System.Collections.Generic;
using System.Text;

namespace ApproxiMATE.ViewModels
{
    public class MainMapViewModel : ViewModelBase
    {
        private readonly IHexagonal _hexagonal;
        //IRequestService _requestService;

        public MainMapViewModel(/*IHexagonal hexagonal IHeatGradient heatGradient,
            IRequestService requestService*/)
        {
            //_hexagonal = hexagonal;
            //_heatGradient = heatGradient;
            //_requestService = requestService;
        }
    }
}
